spring:
  datasource:
    url: jdbc:h2:tcp://localhost/D:\Workspace\jpashop
    username: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
